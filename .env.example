# Example environment / build defines (placeholders only — never commit real secrets)

# GitHub Releases update provider (distribution only; not an app backend)
GITHUB_UPDATE_OWNER=
GITHUB_UPDATE_REPO=GymPulse

# Optional future SyncPort — disabled by default; do not set for Phase 0
# SYNC_ENABLED=false
